A copy of the HUR Lab Editor, for reference only.

It does NOT work as a local file or on your web host: it saves through
claude.ai, so it only runs as a published Claude artifact. Open it from
your artifact list instead.

Its Export button gives you the files in ../content/.
